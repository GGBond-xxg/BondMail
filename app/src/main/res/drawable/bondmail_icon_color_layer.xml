<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Adaptive launchers crop foreground layers outside their safe zone. Keep the artwork at
         the same visual scale as the neighboring Gmail/Thunderbird icons. -->
    <item
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp">
        <bitmap
            android:src="@drawable/bondmail_icon_color"
            android:antialias="true"
            android:dither="true"
            android:filter="true"
            android:gravity="fill" />
    </item>
</layer-list>
