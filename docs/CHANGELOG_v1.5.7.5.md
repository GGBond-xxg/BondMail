# BondMail v1.5.7.5

版本代码：170。支持覆盖安装。

- 优化冷启动首次打开邮件的正文交接：页面提交后等待视觉状态回调，让 WebView 在原生占位层下先绘制，再显示正文并开始转场。
- 正文等待期间显示加载骨架，避免只有标题和一大片空黑区域。
- 保留已加载邮件复用，不等待远程图片下载。

已对照用户录屏定位；尚未进行本版真机复测。

真机补丁：取消加载状态立即放行，将冷启动兜底等待由 450 ms 调整为 1500 ms；当前邮件启用离屏预绘制，退出后关闭；正文由原生占位层遮盖而非切换 WebView 透明度，详情转场开始前隐藏整个目标层。已在连接的 vivo 手机覆盖安装并对首封 Google HTML 邮件进行冷启动录屏检查，最终有效录屏中正文随转场显示，未见原先正文黑屏，整页空白帧检测为 0。原用户 IB 邮件及其他机型尚待复测。版本号保持 1.5.7.5。
