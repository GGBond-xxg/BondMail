# v1.5.5.7

- 新增 MUJI、UNIQLO、Decathlon 品牌图标与名称、域名识别。
- 新增 Hotel、Shopping、ExpressDelivery 通用图标；仅根据发件人名称匹配，具体品牌优先。
- 跟随主题的头像统一使用同一组容器色，修复 MIUIX 邮件列表中亮蓝底白图标与暗底蓝图标混用。列表、详情和邮箱服务商页面共用此配色。
- 保留 Hotel、MUJI 的描边；删除 MUJI 原素材中无尺寸的不可见矩形。

版本名：1.5.5.7；版本代码：159。签名沿用上一版，可覆盖更新。

新 SVG 由用户提供，品牌标识权利归各权利人。原始素材保留于本地 ICON；打包使用 contact_logos 下的整理版本。

验证：55 项 JVM 测试通过；262 个 SVG 通过 XML 检查，无重复文件名、无缺失域名映射。尚未进行本版真机显示验证。
