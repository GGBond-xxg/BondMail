# v0.2.38.0 原始图标、刷新通知、全高加载与抽屉动画专项回归

## 版本与构建

- [ ] `versionName=0.2.38.0`、`versionCode=59`、Room v8、MIME parser v8、HTML `layout-v33`。
- [ ] 从 v0.2.37.0 覆盖安装后，账户、正文缓存、附件、已读/星标、OAuth 与授权码数据全部保留。
- [ ] 本机执行 `clean compileDebugKotlin assembleDebug assemblePerformance --no-daemon` 全部成功。
- [ ] Debug 与 Performance 的图标、通知、加载卡和抽屉动画一致。

## 用户提供的应用图标

- [ ] Manifest 的 `icon`/`roundIcon` 指向新的 `ic_launcher_bondmail` 名称，而不是旧 `ic_launcher`。
- [ ] 普通彩色桌面图标与提供的 `BondMailDefIcon(2).png` 视觉一致，没有再次重绘或改色。
- [ ] Android 13+ 开启主题图标后使用提供的 `BondMailmonaIcon(1).png` 透明轮廓，不回退到旧实心/描边资源。
- [ ] 圆形图标、应用抽屉和启动页都显示同一套图形，无旧图标混用。
- [ ] 启动器仍缓存旧图标时，先移除快捷方式；必要时卸载测试版重装后确认。

## 前台刷新与后台通知

- [ ] 应用停留在收件箱，手动下拉刷新出新邮件：列表更新，但不响铃、不振动、不弹系统横幅。
- [ ] 页面内点击刷新、前台恢复自动刷新和 `syncNow()` 手动 Worker 同样静默消费，不重复通知。
- [ ] 主动刷新与周期 Worker 同时结束时，同一封邮件不会一边出现在列表、一边再通知。
- [ ] 应用退到后台后收到真正的新邮件，使用 `new_mail_alerts_v3` 频道发送通知。
- [ ] 新频道默认有通知声音、两段振动和 HIGH 重要级别；系统允许悬浮通知时显示横幅。
- [ ] Android 13+ 未授权时不崩溃、不偷偷发通知；授权后下一封后台新邮件正常提醒。
- [ ] 在系统中关闭 BondMail 全部通知或关闭新频道后，应用遵从系统设置。
- [ ] 关闭应用内通知后收到的邮件不会在重新开启时批量补弹。

## 全高加载卡

- [ ] 打开从未加载过的邮件，主题和发件人稳定，正文圆角卡填满顶栏与底部操作栏之间的区域。
- [ ] 骨架最多 7 行，40dp 圆形加载动画位于正文可用区域中央，不是底部细线。
- [ ] 已知附件时，加载阶段提前显示附件条；附件名和大小不挤压头部。
- [ ] 浅色和深色模式下加载卡均无四角重阴影，低透明边框仍可辨认。
- [ ] 失败重试仍保留主题、发件人和摘要，底部错误条可点击重试。

## 短/长邮件过渡

- [ ] 一两行纯文本邮件在 HTML 提交后，加载卡先以阻尼弹簧从全高收回，再平滑淡出到最终卡片。
- [ ] 只有附件或正文很短的邮件同样收回，不从屏幕底部瞬间跳到上方。
- [ ] 长 Newsletter/复杂表格邮件保持全高，最终 HTML 完整渐显后自然向下滚动。
- [ ] 固定桌面画布邮件不被错误判为 SHORT，不发生先缩短再立即长高。
- [ ] 1×1、tracking、beacon、spacer 等图片不把短文本邮件误判成长邮件。
- [ ] 重复打开已提交邮件和 A→B→A 直接复用，不重播全高加载、弹簧收回或渐显。
- [ ] 系统关闭动画时直接显示最终高度，不强制播放弹簧。

## 抽屉邮箱展开/收回

- [ ] 账户超过三个时，默认只显示前三个。
- [ ] 点击箭头后，额外账户由顶部向下展开并渐显，不是直线瞬间出现。
- [ ] “添加邮箱”和文件夹入口跟随展开容器连续下移，不跳位置。
- [ ] 收回时额外账户先渐隐再向顶部压缩，后续入口平滑上移。
- [ ] 同一枚向下箭头平滑旋转 180°，不切换两枚图标产生闪烁。
- [ ] 展开状态下拖动排序、点击账户、编辑和删除仍正常。
- [ ] 系统关闭动画后，展开/收回立即完成。

## 回归边界

- [ ] v0.2.37.0 的整页 Q 弹、WebView 原生渐显、同封重开、Grab/Binance/Facebook 排版和动量置顶无回归。
- [ ] CircularRevealSwitch 仍未接入，主题切换不新增 Activity 重建。

---

# v0.2.37.0 图标、整页 Q 弹、HTML 渐显与发件人模板专项回归

## 版本与构建

- [ ] `versionName=0.2.37.0`、`versionCode=58`、Room v8、MIME parser v8、HTML `layout-v32`。
- [ ] 从 v0.2.36.0 覆盖安装后，账户、正文缓存、附件、已读/星标、OAuth 与授权码数据全部保留。
- [ ] 本机执行 `clean compileDebugKotlin assembleDebug assemblePerformance --no-daemon` 全部成功。
- [ ] Debug 与 Performance 中图标、Q 弹、渐显、Grab/Facebook 排版和置顶动画一致。

## 启动图标

- [ ] 普通彩色图标是缩进的圆角描边信封，视觉面积不再明显大于 Gmail/Thunderbird。
- [ ] Android 13+ 开启主题图标后，使用独立 monochrome 前景，不出现实心黑块或过粗横条。
- [ ] 圆形/方形启动器、应用列表和启动页中的信封比例一致，无边缘裁切。
- [ ] 覆盖安装仍显示旧图标时，移除桌面快捷方式后重新添加即可刷新，应用数据不受影响。

## 整张邮件 Q 弹

- [ ] 正文位于顶部时，从 HTML 区域向下拉，主题、头像、发件人、邮箱、日期和正文一起移动。
- [ ] 从主题、头像、发件人或日期开始向下拉，产生相同的阻尼距离和回弹。
- [ ] 越接近约 60dp 极限阻力越大，松手后使用弹簧回位，不匀速、不抖动。
- [ ] 页面移动过程中手势方向稳定，不因 View 自身位移出现反向跳动。
- [ ] 正文已经离开顶部时，下拉恢复正常滚动，不错误触发整页 Q 弹。
- [ ] 上滑收起顶部/底部 Chrome、正文链接、图片缩放和横向手势无回归。

## K-9 风格正文渐显

- [ ] 冷开未缓存邮件时先保持可读纯文本预览，HTML 未完成前完全不可见。
- [ ] 最终 HTML 作为整张页面在约 260ms 内连续渐显并轻微上移，不出现“先露半页、再突然闪全”的两阶段变化。
- [ ] 含远程图片邮件不会因 `onPageCommitVisible` 提前露出未排版完成的内容。
- [ ] 返回后精确重开同一封已提交邮件直接显示，不重播动画。
- [ ] 已看过但 WebView 被第三封邮件淘汰时，只进行约 150ms 轻渐显。
- [ ] 快速 A → B → A 不继承上一封 WebView 的 alpha、translationY 或旧正文。
- [ ] 系统关闭动画后，最终 HTML 直接切换，不强制播放渐显。

## 纯文本/短邮件阴影

- [ ] 未加载纯文本邮件的四个圆角不再有明显黑色投影。
- [ ] 低透明边框仍能区分卡片与页面背景。
- [ ] 最终 HTML 卡片阴影与占位阶段接近，不在替换时突然变重。
- [ ] 一两行短邮件仍按自然高度收拢，底边动画不跳变。

## Grab 与 Binance

- [ ] 打开 Grab “希望这个告别不是永久的”邮件，绿色头图、标题和正文完整显示在屏幕内。
- [ ] Grab 正常 `@grab.com` 与 `no-reply_at_grab_com_...` 转义地址均进入固定画布缩放路径。
- [ ] Grab 无右侧裁切、横向滚动、巨大单列或只显示左半边的问题。
- [ ] Binance 真响应式模板继续使用手机 FLUID 布局；固定旧模板继续整体缩放。
- [ ] Binance 社交图标、防钓鱼码和免责声明保持正常，修 Grab 不得破坏 Binance。

## Facebook/Meta 邮件

- [ ] 打开 `registration@facebookmail.com` 等注册/安全邮件，正文比原邮件明显更可读。
- [ ] Facebook/Meta 横向字标约 128px、方形标约 52px，比例正确、不模糊拉伸。
- [ ] 1×1/2×2 跟踪图不被误放大。
- [ ] 普通正文照片、按钮图、社交图标及非 Facebook 发件人不受 132% 专用缩放影响。
- [ ] 浅色和算法深色模式下品牌图、文字与链接均可辨认。

## 置顶动量

- [ ] 在几十封邮件处点击置顶，动画先快后慢并连续停在第一封。
- [ ] 在几百封邮件处点击置顶，中间长距离不会持续匀速拖动或逐行卡顿。
- [ ] 最后几屏明显减速，最终精确为 `index=0 / offset=0`，按钮随后消失。
- [ ] 距离顶部很近时不发生可见跳转，只做短减速收尾。
- [ ] 首页和联系人列表表现一致；系统关闭动画时立即置顶。

## 主题动画边界

- [ ] 本版未接入 CircularRevealSwitch，设置页主题切换不新增 Activity 重建，也不销毁当前邮件 WebView。

---

# v0.2.36.0 长标题、头部拖动、重复打开与短邮件专项回归

## 版本与构建

- [ ] `versionName=0.2.36.0`、`versionCode=57`、Room v8、MIME parser v8、HTML `layout-v31`。
- [ ] 从 v0.2.35.1 覆盖安装后，账户、正文缓存、附件、未读/星标、OAuth 与授权码数据全部保留。
- [ ] 本机执行 `clean compileDebugKotlin assembleDebug assemblePerformance --no-daemon` 全部成功。
- [ ] Debug 与 Performance 均能进入同一封邮件，且表现一致。

## 长主题与发件人几何

- [ ] 打开“【币安】USDC充值成功提醒 - 2026-07-26 14:27:14 (UTC)”一类长主题，主题不超过三行。
- [ ] 长主题使用比上一版更紧凑的字号，发件人卡片从主题结束后开始，不与主题、头像或邮箱重叠。
- [ ] 一行短主题保持清楚但不过分放大；两行主题和三行主题的字号过渡自然。
- [ ] 头像约 46dp，发件人、邮箱、收件账户与日期层级清楚，邮箱和收件账户过长时单行省略。
- [ ] 从摘要切换为 HTML 时，主题、发件人卡片和正文起点完全不跳高、不改变换行。
- [ ] 系统字体分别设为默认、大号和更大档位，主题与发件人区域仍不重叠。
- [ ] 附件稍后解析出来时，只显示预留的回形针，不挤压日期或发件人名称。

## 从标题/头像开始滚动

- [ ] 手指从主题文字开始上滑，正文连续滚动，顶部和底部 Chrome 按原规则收起。
- [ ] 手指从头像、发件人、邮箱、收件账户和日期分别开始上滑，均可滚动正文。
- [ ] 快速甩动头部区域后仍有 WebView 惯性，不在松手后立即停止。
- [ ] 头部滚出屏幕后，其旧触摸区域不会继续挡住正文链接、图片或按钮。
- [ ] 从正文开始滚动、缩放、点击链接的行为与 v0.2.35.1 一致。

## 重复打开与快速切换

- [ ] 首次打开 A，返回后立即再次打开 A，不重新显示只有标题/摘要的初始实体，不出现白帧或第二次正文动画。
- [ ] 打开 A → 返回 → 打开 B → 返回 → 再打开 A，A 的已提交页面优先恢复，正文不得错误显示 B。
- [ ] 连续重复 A 至少五次，主题、发件人和正文都不出现字体闪动或几何变化。
- [ ] 正文加载完成后立即返回再重开，完整 MIME 快照已经可用，不依赖 Room 延迟回调。
- [ ] 星标、已读/未读状态在重开时使用列表最新值，但 HTML、纯文本、附件和内容哈希不被丢弃。
- [ ] 第三个不同邮件淘汰旧 WebView 后再打开已看过的邮件，只允许一次约 120ms 轻交叉渐显，不硬切闪白。
- [ ] Debug 日志中的 `document reuse retained=true` 只对应当前邮件内容键，不短暂复用上一封正文。
- [ ] 首次未缓存未读邮件仍保持 v0.2.35.0 的 single-flight、pending `\Seen` 与失败重试行为。
- [ ] WebView 渲染进程异常退出后不会复用死亡实例，重试仍能显示当前邮件。

## 短邮件与附件邮件

- [ ] 正文只有一两行时，加载占位卡不再填满剩余屏幕。
- [ ] 最终短正文出现时，内容卡底边自然落位，不从屏幕底部瞬间跳到上方。
- [ ] 摘要长度变化或附件信息补齐时，卡片尺寸用约 240ms 平滑调整。
- [ ] 只有附件、正文为空的邮件在加载阶段就显示近似最终高度的附件条。
- [ ] 长正文仍可正常滚动，不因短邮件优化而裁切或限制高度。
- [ ] 系统关闭动画后，尺寸与正文切换直接完成，不强制播放动画。

## Binance 与通用 HTML

- [ ] Binance 模板包含真实 `max-width` 媒体查询时采用手机宽度，正文不再像整页缩略图。
- [ ] Binance 固定旧模板没有手机媒体查询时仍保留完整 600px 画布，Logo、按钮、防钓鱼码、免责声明和版权不被拆散。
- [ ] 固定桌面画布的正文可读性比 v0.2.35.1 更好，且无右侧裁切或横向滚动。
- [ ] X、Telegram、Facebook、LinkedIn、YouTube、Reddit、Instagram 图标保持横排。
- [ ] Neverless 等原生手机长邮件仍保持正常字号和宽度。
- [ ] 163/126、GitHub、Cloudflare、纯文本和引用邮件均无新增裁切、超宽或字体异常。
- [ ] 浅色与深色模式下卡片、正文、链接和品牌色可读，算法夜间模式仍生效。

## 主题动画边界

- [ ] 本版设置页主题切换行为与 v0.2.35.1 一致，没有接入 CircularRevealSwitch，也不因邮件修复新增 Activity 重建。

---

# v0.2.35.1 Compose 编译热修复

- [ ] `versionName=0.2.35.1`、`versionCode=56`、Room v8、MIME parser v8、HTML `layout-v28`。
- [ ] `DetailScreen.kt` 的错误提示 Surface 使用 `padding(start = 24.dp, end = 24.dp, bottom = 96.dp)`。
- [ ] 本机执行 `clean compileDebugKotlin assembleDebug assemblePerformance --no-daemon` 全部成功。
- [ ] 断网打开未缓存邮件时，底部错误/重试卡左右间距与底部间距和 v0.2.35.0 保持一致。

---

# v0.2.35.0 首封未读、稳定头部与最终 HTML 帧专项回归

## 版本与覆盖安装

- [ ] `versionName=0.2.35.0`、`versionCode=55`、Room v8、MIME parser v8、HTML `layout-v28`。
- [ ] 从 v0.2.34.0 覆盖安装后，账户、邮件、正文、附件、已读/星标、OAuth 和授权码数据均保留。
- [ ] 本机执行 `clean compileDebugKotlin assembleDebug assemblePerformance --no-daemon` 全部成功。

## 第一封未读邮件

- [ ] 冷启动后首先点击一封未缓存的未读邮件，第一次即可打开，不出现无限骨架、连接失败后自动重复请求或必须先打开第二封的情况。
- [ ] Gmail、Outlook/Hotmail、163/126 分别测试至少一封未读邮件。
- [ ] 点击后列表立即显示已读；等待后台同步后网页邮箱同样显示已读。
- [ ] 正文读取与远端 `\Seen` 不产生两个并发 BODY 请求；Logcat 中同一 UID 的列表点击与详情只共享一次交互正文结果。
- [ ] 正在后台预取第一封邮件时立即点击它，交互 BODY 不等待阻塞式预取结束，也不复用预取 Store。
- [ ] BODY 读取过程中触发下拉刷新/前台同步，服务器旧 FLAGS 不会把本地邮件重新显示为未读；正文成功后网页邮箱最终变为已读。
- [ ] 打开过程中主动返回，再次进入同一封邮件仍能正常读取，不残留永久占用的任务。
- [ ] 打开后立刻手动标记未读，延迟到达的旧 `\Seen` 任务不能再次覆盖该选择。
- [ ] 点击系统通知/深链直接进入一封未读邮件，在没有列表首帧快照的情况下仍能加载正文并正确标记已读。

## 标题与发件人稳定性

- [ ] 导航第一帧已经显示当前邮件的主题、头像、发件人、地址、收件账户和日期。
- [ ] 从摘要切换为最终 HTML 时，上述文字不改变字号、字重、宽度、换行或垂直位置。
- [ ] 附件信息稍后解析出来时只出现回形针，不挤压日期和发件人名称。
- [ ] 滚动正文时，原生主题/发件人层与 HTML 正文同步移动，不漂移、不悬空、不盖住正文。
- [ ] 系统字体设为默认、大号和最大可用档位时，正文起点没有明显跳动。
- [ ] 主题、发件人、地址和日期在摘要/HTML 两阶段保持相同字距，不在临界宽度处突然多换一行。

## 正文揭示与异常

- [ ] 初次打开保持可读摘要，最终 HTML 只进行一次短渐显；不出现“摘要消失 → 灰页/转圈 → HTML”的第二次闪动。
- [ ] 返回后立即重开同一封邮件，保留页面挂载后直接显示，不露出空白 WebView。
- [ ] 远程图片邮件等待排版稳定后显示，不先展示未完成布局再整体变形。
- [ ] 断网打开未缓存邮件时仍保留头部和摘要，底部出现连接失败/重试条，而不是整页空白。
- [ ] 恢复网络点击重试后成功显示 HTML；远程图片按钮、链接、附件、回复、转发、分享和删除保持可用。

## HTML 排版回归

- [ ] Binance USDC 邮件按约 600px 完整画布整体缩放，Logo、按钮、防钓鱼码、免责声明和版权完整。
- [ ] X、Telegram、Facebook、LinkedIn、YouTube、Reddit、Instagram 等独立小 table 图标保持横排。
- [ ] 163/126 手机事务邮件仍为手机宽度；Cloudflare 等桌面 Newsletter 仍整体缩放。
- [ ] 深色模式继续自动适配；浅色/深色切换后正文和隐藏头部占位的几何一致。

---

# v0.2.34.0 双闪与 Binance 排版专项回归

## 版本与覆盖安装

- [ ] `versionName=0.2.34.0`、`versionCode=54`、Room v8、MIME parser v8、HTML `layout-v26`。
- [ ] 从 v0.2.33.1 覆盖安装后，账户、邮件、正文、附件、已读/星标、OAuth 和授权码数据均保留。
- [ ] 本机执行 `clean compileDebugKotlin assembleDebug assemblePerformance --no-daemon` 全部成功。

## 首次打开只显示一次

- [ ] 冷启动后点击一封未打开过的新邮件，目的页第一帧已有当前邮件的主题、头像、发件人、地址、日期和正文摘要，不出现整页白/空。
- [ ] 最终 HTML 只进行一次短渐显；不得出现“空页一次、壳层一次、正文再一次”的连续两次闪动。
- [ ] 从点击到正文稳定期间，顶部栏和底部 Dock 不闪烁、不改变背景色。
- [ ] 主题、发件人和邮箱从原生壳层切换到 WebView 时不明显跳位置、粗细或字号。
- [ ] Android 系统开启“移除动画”后，正文直接切换，不强制播放 180ms 渐显。

## 缓存重开与快速切换

- [ ] 返回列表后立即重开同一封邮件，保留 WebView 页面直接显示，不出现空白挂载帧或重复渐显。
- [ ] 连续打开 A、返回、打开 B，任何时刻都不得短暂显示 A 的正文或发件人。
- [ ] 快速返回时，旧页面延迟回调不会覆盖列表或下一封详情。
- [ ] WebView 渲染进程退出后自动恢复一次；恢复期间仍显示当前邮件原生壳层。

## Binance 邮件

- [ ] 打开 `do-not-reply@ses.binance.com` 的 USDC 充值成功邮件，Logo、标题、正文、按钮、反钓鱼码、免责声明和版权信息完整。
- [ ] “保持联系”下的 X、Telegram、Facebook、LinkedIn、YouTube、Reddit、Instagram 图标保持横向一排，不再竖直排列。
- [ ] 正文按完整画布整体缩放，无右侧裁切、横向滚动、按钮变形或文字挤成窄列。
- [ ] 深色模式继续自动适配；正文和链接可读，品牌黄色保留，背景不突然变白。
- [ ] 浅色模式与 Apple Mail 的结构顺序一致；允许保留 BondMail 自己的 Gmail 风格外层主题/发件人内容卡。

## 通用 HTML 回归

- [ ] 163/126 事务邮件仍使用手机宽度，不回退为中央小卡片。
- [ ] Cloudflare/Newsletter 桌面画布仍整体缩放，无右侧裁切。
- [ ] GitHub、Bybit、Grab、纯文本、引用邮件的换行、图片、按钮和滚动正常。
- [ ] 其他包含 3～12 个小图片/SVG 的社交或支付图标行保持横排；普通正文表格不被误判。
- [ ] 远程图片禁用/允许按钮、链接外跳、附件卡和底部回复/转发/分享/删除功能不受影响。

---

# v0.2.33.1 附件、Sent 与首次展示

- [ ] `versionName=0.2.33.1`、`versionCode=53`、Room v8、MIME parser v8、HTML `layout-v25`。
- [ ] 首次打开未缓存邮件：正文可见帧完成后保持背景 300ms，再渐显 300ms。
- [ ] 第二次打开同一封邮件：直接复用缓存，不重复等待和渐显。
- [ ] 带附件邮件在 Outlook 已发送和 163 收件箱均显示正文、回形针、文件名与大小。
- [ ] 已发送页只有一条记录：正在发送完成后替换为正式远端行。
- [ ] 删除已发送邮件后刷新、重进应用均不恢复。
- [ ] 旧版带附件邮件首次重开会以 parser v8 重新解析并补齐元数据。

# v0.2.31.0 已读、预测性返回、已发送/草稿专项回归

## 构建与升级

- [ ] `versionName=0.2.31.0`、`versionCode=49`、Room v7、HTML `layout-v24`。
- [ ] `compileDebugKotlin`、`assembleDebug`、`assemblePerformance`、`minifyPerformanceWithR8` 全部成功。
- [ ] 从 v0.2.30.0 覆盖安装后账户、邮件正文、OAuth、授权码和通知选择保留。

## 打开即已读

- [ ] 点击未读邮件后立刻返回，列表已经是已读，不需要左滑或等待刷新。
- [ ] Gmail、Outlook/Hotmail、163 分别等待至少一次后台同步，邮件不会恢复未读。
- [ ] 断网打开邮件时本地保持已读；恢复网络再次打开可补交服务器 `\Seen`。
- [ ] 在网页邮箱刷新后，该邮件同样显示已读。
- [ ] 手动左滑“标记未读”仍能覆盖之前的打开已读状态。

## Telegram 风格与预测性返回

- [ ] 打开详情时目的页不透明短距离进入，下层列表文字不与详情叠加。
- [ ] 点击返回时详情快速完整滑向右侧，无长尾拖拉。
- [ ] Android 手势返回时详情随手指进度移动；取消手势后顺滑复位，完成手势后快速收尾。
- [ ] 服务商选择、邮箱配置、邮件详情使用一致的返回方向和速度。
- [ ] 返回过程中正文 WebView 不闪白，标题/发件人不出现粗细变化。

## 未读入口

- [ ] 顶部文件夹顺序为：收件箱、未读邮件、星标、已发送、草稿箱、垃圾邮件。
- [ ] 抽屉同样包含未读邮件。
- [ ] 打开未读邮件后，它从未读入口即时移除。
- [ ] 下拉刷新未读入口时同步 INBOX，不创建伪远端文件夹。

## 已发送

- [ ] 进入已发送会读取 163/Gmail/Outlook 网页端已有的已发送邮件。
- [ ] 点击发送后立即出现一条“正在发送”记录。
- [ ] SMTP 成功后该记录以动画变为“已发送”，随后与服务器 Sent 副本去重。
- [ ] SMTP 失败显示“发送失败”，不会误显示已发送。
- [ ] 模拟 Sent APPEND/同步失败时，邮件不会被 SMTP 重复投递。
- [ ] 在网页邮箱删除最近一封 Sent 后，重新进入/刷新已发送，本地最近窗口同步删除。

## 草稿

- [ ] 仅填写收件人或主题后返回，会出现保存/舍弃确认。
- [ ] 选择保存后草稿立即出现在草稿箱，红色“草稿”标签清晰。
- [ ] 等待网络同步后在 163/Gmail/Outlook 网页端 Drafts 中能看到同一草稿。
- [ ] 点击本地或服务器草稿都复用同一个写邮件 Bottom Sheet。
- [ ] 修改并再次保存时服务器不产生重复草稿，旧 UID 在新 APPEND 成功后删除。
- [ ] 选择舍弃后本地和服务器草稿均删除。
- [ ] 发送草稿时先显示正在发送；SMTP 成功后草稿消失并进入已发送；失败时草稿仍可恢复。
- [ ] 在网页邮箱新增/删除最近草稿后，进入草稿箱能完成有界对账。

## 详情闪烁

- [ ] 首次打开未缓存邮件时显示稳定骨架，正文以短渐显/轻缩放覆盖，不发生单帧白闪。
- [ ] 返回后再次打开同一封邮件，保留 WebView 复用，仍无整页闪烁。
- [ ] Cloudflare、GitHub、网易和 Bybit 的宽度、图片、滚动与 v0.2.30.0 一致。

---

# v0.2.30.0 已读竞态与详情首帧专项回归

## 版本与构建

- [ ] `versionName=0.2.30.0`、`versionCode=48`、Room v6、HTML `layout-v24`。
- [ ] `compileDebugKotlin`、`assembleDebug`、`assemblePerformance` 与 `minifyPerformanceWithR8` 均成功。
- [ ] 从 v0.2.29.1 覆盖安装后账户、邮件、正文缓存、OAuth、授权码和通知选择保留。

## 已读/未读同步竞态

- [ ] 同时保留 Gmail、Outlook/Hotmail 和 163 账户，等待周期同步开始后立即打开一封未读邮件。
- [ ] 邮件返回列表后保持已读，不会在同步完成、正文预取完成或数秒后重新变成未读。
- [ ] Debug 日志出现 `read flag optimistic`、`seen success` 和 `read flag committed`。
- [ ] 同步与打开重叠时可出现 `skip stale flag snapshot`，且该批旧 FLAGS 不改变列表状态。
- [ ] 手动切换已读/未读连续执行两次，最终本地与服务器一致。
- [ ] Outlook/Gmail/163 分别重启应用并刷新，已读状态仍与服务器一致。
- [ ] 星标操作在同步重叠时同样不会被旧 FLAGS 撤销。
- [ ] 断网执行已读或星标操作时，本地状态回滚并记录 `read flag rollback`，恢复网络后可再次操作。

## 详情字体与重新打开

- [ ] 首次打开未缓存邮件时，加载阶段只显示中性骨架，不显示另一套真实主题/发件人文字。
- [ ] Chromium 正文出现时不再发生主题或发件人“粗 → 细”。
- [ ] 返回列表后立即重开同一封邮件，没有一帧全白/全空详情；保留页面重新挂载后一次显示。
- [ ] 快速连续点击不同邮件，目的页首帧始终有稳定骨架，不显示上一封内容，也不出现空顶栏下的大块闪帧。
- [ ] Debug 日志可见 `document reuse retained=true`，且复用后内容与当前 messageId 一致。
- [ ] Cloudflare、GitHub、网易、Bybit 的正文宽度、图片、滚动和底部 Dock 与 v0.2.29.1 一致。

---

# v0.2.29.1 详情首帧专项回归

## 版本

- [ ] `versionName=0.2.29.1`、`versionCode=47`、Room v6、HTML `layout-v24`。

## 首次打开

- [ ] 点击未打开过的邮件，详情从第一帧开始保持不透明。
- [ ] 主题、发件人名称和邮箱不再出现“粗 → 细”的变化。
- [ ] 邮件列表文字不会透过详情页与主题/发件人重叠。
- [ ] 正文尚未完成时可显示稳定占位，但最终切换时字体位置和字重保持一致。

## 再次打开

- [ ] 返回列表后立即重开同一封邮件，已提交页面直接显示。
- [ ] 不出现整页闪白、邮箱列表残影或短暂加载进度。
- [ ] 打开另一封邮件后再回来，正文缓存正常且不会错误显示上一封内容。

## 动画与回归

- [ ] 前进/返回保留轻量水平位移，但没有整页透明度淡入淡出。
- [ ] Cloudflare、GitHub、网易和 Bybit 的宽度、图片、滚动保持 v0.2.29.0 结果。
- [ ] 顶栏账户名称、星标、更多和底部 Dock 行为不变。

---

# v0.2.29.0 专项回归

## 构建与升级

- [ ] `assembleDebug`、`assemblePerformance` 与 `minifyPerformanceWithR8` 均成功。
- [ ] 从 v0.2.28.1 覆盖安装后账户、邮件、正文缓存、联系人、OAuth 和通知选择仍存在。
- [ ] `versionName=0.2.29.0`、`versionCode=46`、Room v6、HTML `layout-v23`。

## 详情顶部与文字稳定

- [ ] 顶部栏显示当前邮件所属账户的显示名称，返回、星标、更多按钮位置不变。
- [ ] 账户显示名称最长 12 个字符；添加和编辑界面的计数显示 `/12`。
- [ ] 旧版本已保存的超长显示名称在启动后截短，不影响账户、凭证和邮件。
- [ ] 首次打开未缓存邮件时，主题和发件人不会在淡化过程中出现“粗 → 细”闪烁。
- [ ] 正文完成后一次稳定切换；无白屏、重复主题或短暂双层文字。

## Gmail 内容区域

- [ ] 主题在页面背景上完整多行显示。
- [ ] 发件人、日期、邮箱地址、收件账户和正文位于同一张 24dp 圆角内容卡。
- [ ] 内容卡在浅色、深色和 Monet 主题下均比页面背景更突出，边框与阴影不过重。
- [ ] 发件人名称过长时单行省略，右侧日期保持可见。
- [ ] Cloudflare 桌面 Newsletter 仍完整显示，无右侧裁切或圆角异常。
- [ ] GitHub 纯文本、网易事务邮件和 Bybit 邮件均位于相同外层卡片，不改变既有宽度适配。
- [ ] 顶部操作按钮和底部回复/转发/分享/删除 Dock 与 v0.2.28.1 一致。

---

# v0.2.28.1 专项回归

## 构建与覆盖安装

- [ ] `assembleDebug`、`assemblePerformance` 与 `minifyPerformanceWithR8` 均成功。
- [ ] 从 v0.2.27.0 覆盖安装后账户、邮件、正文缓存、联系人、OAuth 和通知选择仍存在。
- [ ] `versionName=0.2.28.1`、`versionCode=44`、Room v6、HTML `layout-v22`。

## Gmail 风格详情内容

- [ ] 详情顶部栏与页面背景颜色一致，没有独立色块或阴影。
- [ ] 返回、星标、更多按钮的位置和点击行为与 v0.2.27.0 相同；底部 Dock 不变。
- [ ] 顶部栏不再显示单行截断主题；主题在正文顶部以多行大标题完整显示。
- [ ] 主题下方显示头像、发件人、发件地址和当前收件账户，邮件原始正文从其下方开始。
- [ ] 正文未下载时立即显示真实主题和发件人，进度只出现在正文区域；最终 WebView 渐显时不明显跳字重或位置。
- [ ] Cloudflare、GitHub、网易、Bybit 与纯文本邮件均使用同一详情头部，不按品牌切换 UI。

## 正文打开一致性

- [ ] 清空数据首次同步后，最近 32 封、且不超过 128 KiB 的小正文按 8 封一批预热。
- [ ] 已有账户冷启动后，界面稳定约 1.8 秒开始预热最近 12 封，不再等待 20 秒。
- [ ] 首页停在某一段邮件列表约 1 秒后，当前可见邮件及少量前后项进入预热；快速滚动时不连续产生请求。
- [ ] 点击尚未预热的邮件时，当前邮件优先于后台正文预取；不出现同一邮件双重下载。
- [ ] 返回后立即重新打开同一封邮件，Debug 日志出现 `document reuse retained=true`，正文几乎立即出现。
- [ ] 连续打开不同邮件时，已下载 Body 仍命中 Room；HTML 准备缓存最多保留 64 份，不因品牌不同决定速度。
- [ ] 大 Newsletter 仍按需下载；首次打开时间可因正文大小/服务器不同而变化，但不会影响邮件列表或其他邮件预热。

## HTML layout-v22 回归

- [ ] Cloudflare 桌面 Newsletter 仍完整显示，无右侧裁切。
- [ ] 两封网易事务邮件继续使用手机宽度，不回退为中央小卡片。
- [ ] GitHub 短邮件继续容易拖动、链接正确换行。
- [ ] 主题移动到内容区后，FLUID 与 DESKTOP_SCALED 邮件的原始正文都不会覆盖主题/发件人区域。
- [ ] 深色、浅色、Monet 三种主题下，详情顶部栏、主题区和发件人区背景一致且文字可读。

---

# v0.2.27.0 专项回归

## 构建与覆盖安装

- [ ] `assembleDebug`、`assemblePerformance` 与 `minifyPerformanceWithR8` 均成功。
- [ ] 从 v0.2.26.0 覆盖安装后账户、邮件、正文缓存、联系人与通知选择仍存在。
- [ ] `versionName=0.2.27.0`、`versionCode=43`、Room v6。

## 网易首次同步

- [ ] 清空数据添加 163/126 邮箱，保存后邮件仍按 8 封逐条、后续 4 封一组进入列表。
- [ ] Debug 日志包含单条 `header window ... fetch=...ms`，不出现排序阶段逐封 UID 网络查询。
- [ ] 与 v0.2.26.0 同网络对比，保存账户到第一封邮件出现的等待明显缩短。
- [ ] 首次历史邮件不发送系统新邮件通知。

## 正文预取

- [ ] 首次同步后最近 24 封、且不超过 128 KiB 的正文按 8 封一批预热。
- [ ] Cloudflare、GitHub、网易安全提醒等最近邮件第一次打开等待明显缩短。
- [ ] 用户主动打开尚未预取的邮件时，当前邮件优先于后台预取。
- [ ] 返回再次打开同一封邮件出现 `body cache hit`，不再重新下载。

## HTML layout-v21

- [ ] Cloudflare 大 Newsletter 仍完整显示 Logo、图表、正文和页脚，无右侧裁切。
- [ ] GitHub 短邮件保持完整换行和容易拖动。
- [ ] `member@service.netease.com` 安全管家邮件不再缩成中央小卡片，主内容接近手机可用宽度。
- [ ] `safe@service.netease.com` 新设备提醒同样使用手机宽度，第二次打开不复用旧错误布局。
- [ ] Bybit 防钓鱼码附近的图片/盾牌符号优先显示彩色 Emoji 或真实资源；缺失时不影响正文排版。
- [ ] Debug 日志包含 `layout selected=...` 和 `fluid compact expanded ...`，且不包含 Token/授权码。

## WebView 渲染进程恢复

- [ ] 正常打开 HTML 邮件不会增加第二个常驻 WebView。
- [ ] 系统回收 WebView 沙箱进程后，当前邮件最多自动重试一次。
- [ ] 死亡 WebView 不会回到复用池；连续失败显示“邮件内容加载失败/重新加载”。
- [ ] 返回列表再打开其他 HTML 邮件可以正常渲染。

## 通知动效与设置

- [ ] 点击“拒绝通知”后提示卡平滑收起，欢迎卡/邮件列表连续上移，无双重弹跳。
- [ ] 点击“允许通知”后仅在用户动作后出现系统权限窗口，返回后的补位同样连续。
- [ ] “拒绝通知”为低强调按钮，“允许通知”为主按钮。
- [ ] 设置页已允许状态使用中性色，看起来不是可点击操作；未允许时“去授权”仍可点击。

---

# BondMail 真机测试清单

> 当前交付基线：v0.2.30.0。先执行文档顶部的 v0.2.30.0 专项回归，再按需要抽查历史功能。

## v0.2.26.0 专项回归

### 通知权限提示与列表补位动画

- [ ] Android 13+ 清除应用数据后打开 BondMail，不自动出现系统通知权限窗口。
- [ ] 应用内提示显示“拒绝通知 / 允许通知”，不再显示“稍后”。
- [ ] 点击“拒绝通知”后提示卡收起，下面的欢迎卡或邮件列表连续上移动画补位，不直接闪现；重启后不再重复显示提示。
- [ ] 点击“允许通知”时才出现系统权限窗口；无论系统结果为允许或拒绝，窗口关闭后下面内容连续上移，不跳帧。
- [ ] 已有账户与无账户欢迎页分别测试一次提示展开/收起，均没有 LazyColumn 位置突变。
- [ ] 设置页出现“权限 → 消息通知”：已授权显示“允许”且点击无动作；未授权显示“去授权”并进入当前应用通知设置。

### 授权码/密码输入

- [ ] 添加 163/126/QQ/iCloud/Yahoo 时，授权码字段请求密码输入类型，不显示明文建议。
- [ ] 输入法右下角显示完成动作；点击后键盘收起，页面可继续滚动并点击保存。
- [ ] 抽屉账户编辑中的“更改授权码/密码”字段具有相同键盘和收起行为。
- [ ] 正确授权码继续执行 IMAP + SMTP 双协议验证；错误值不替换旧凭证。

### HTML layout-v20 与图标

- [ ] Cloudflare 长 Newsletter 继续完整显示，未因本版 FLUID 主卡片扩展而回退。
- [ ] GitHub 纯文本/短 HTML 邮件继续正常滚动与断行。
- [ ] 网易安全提醒等窄主卡片邮件相较 v0.2.25.0 更合理利用手机宽度，不被误判为桌面 Newsletter。
- [ ] 含 `data-src`、`data-original`、`data-lazy-src`、`data-cfsrc` 或 `data-srcset` 的邮件图标/图片可加载。
- [ ] 使用 `//cdn...` 的图片、背景图和 CSS `url()` 能按 HTTPS 加载。
- [ ] 小型图标不会被强制 `height:auto` 后压缩成 0；大图仍不会超过屏幕宽度。
- [ ] 远程图片策略设为“从不”时仍阻止外部资源；设为“始终”或 Wi-Fi 条件满足时才加载。

### 同步与正文速度

- [ ] 首次同步仍采用批量 Header FETCH，最新邮件渐进进入列表，不为逐行动画拆成多次网络请求。
- [ ] Header 行先出现，正文预览和邮件内容随后补齐；历史邮件不批量发送通知。
- [ ] 最近 12 封不超过 128 KiB 的小正文在首次同步后较 v0.2.25.0 更早进入预取。
- [ ] 大 Newsletter 不阻塞首屏邮件列表，仍在打开时按需加载。
- [ ] WebView 预热不阻塞冷启动和首个列表手势；首次打开 HTML 邮件没有明显 Chromium 白屏。

### 首页顶栏与构建

- [ ] 首页左上抽屉按钮没有可见圆形底色，但点击范围、Ripple 和滚动收起动画正常。
- [ ] `assembleDebug` 与 `assemblePerformance` 均通过；Performance R8 不再报告 Nimbus/Tink/Bouncy Castle 可选类缺失。

---

## v0.2.25.0 专项回归

### 邮件正文与详情加载

- [ ] 清除/覆盖安装后打开 HTML、纯文本和 Newsletter 邮件，均不再永久停留在骨架页。
- [ ] Debug 日志出现 `body success` 后能继续出现 `document load`；不得再出现 `PatternSyntaxException` 或 `MailWebViewCache.<clinit>`。
- [ ] 顶栏底部与发件人骨架/实际发件人区域连续，不再出现额外约 12dp 空白断层。
- [ ] 发件人区域与正文之间约 8dp，加载骨架和最终 HTML 位置一致。
- [ ] 人为制造 HTML 准备/主文档失败时显示“邮件内容加载失败”和“重试”，不永久显示加载条。
- [ ] 点击重试可重新准备并提交当前邮件，不改变已读、星标或本地正文。

### 账户编辑

- [ ] 授权码账户编辑框显示“更改授权码/密码”，单行完整显示。
- [ ] 留空只修改显示名称；错误新凭证不替换旧值；正确新凭证通过 IMAP/SMTP 后保存并重新同步。

### Performance / R8

- [ ] 执行 `assemblePerformance`，`minifyPerformanceWithR8` 不再报告 Tink/Bouncy Castle 缺失类。
- [ ] performance APK 可启动、打开 Microsoft 登录、进入邮件详情并显示 HTML 正文。
- [ ] R8 后 WorkManager、JavaMail、MSAL 与 Google 授权回调仍正常。

### 回归保留

- [ ] 首次 163 同步仍先显示第一封，再分批补齐列表；历史邮件不批量通知。
- [ ] Outlook/Hotmail/Live 登录、收信和发信保持正常。
- [ ] Gmail 403 测试用户/应用验证问题按 Google Cloud 控制台处理，不作为本版正文修复阻塞项。

---

## v0.2.24.0 专项回归

### 163/126 与 App Password 更新

- [ ] 使用已有 163 账户打开抽屉编辑窗口，显示名称可修改，并出现“更新客户端授权码 / App 专用密码”。
- [ ] 授权码留空保存时只修改显示名称，原凭证和邮件仍可使用。
- [ ] 输入错误的新授权码，IMAP/SMTP 验证失败；旧凭证、账户和本地邮件不被删除或替换。
- [ ] 输入网页端新生成的正确授权码，收件和发件验证均通过后保存；随后该账户自动安静刷新。
- [ ] 126、QQ、iCloud、Yahoo 分别抽查一次原地更新；日志不打印新旧凭证。

### Gmail OAuth

- [ ] 测试 APK 的签名 SHA-1 与 Google Cloud Android OAuth Client 完全一致。
- [ ] 从 Gmail 服务商入口点击“使用 Google 继续”，可选择账户并显示 Gmail 权限同意页面。
- [ ] 同意后先通过 IMAP 与 SMTP 验证，再进入邮箱；历史邮件作为基线，不批量发送新邮件通知。
- [ ] 收件、打开正文、已读、星标、删除和发件均使用 XOAUTH2 成功。
- [ ] 重启应用后不再次要求输入密码；后台同步能重新取得短期 Access Token。
- [ ] 在 Google 账户页面撤销 BondMail 权限后，同步提示重新授权；抽屉编辑窗口重新授权后保留全部本地邮件。
- [ ] 重新授权时故意选择另一 Gmail 账户，显示账户不匹配，不覆盖当前本地账户。
- [ ] 已存在相同邮箱时再次添加会提示重复账户。

### Outlook / Hotmail / Live OAuth

- [ ] Microsoft Entra 中登记的包名、签名哈希与测试 APK 一致，Redirect URI 可正确回到 BondMail。
- [ ] Outlook、Hotmail、Live 或 Outlook.jp 至少各抽查一个可用账户入口/别名。
- [ ] MSAL 页面可选择个人 Microsoft 账户；登录后 IMAP 与 SMTP XOAUTH2 验证成功。
- [ ] 收件、正文、已读、星标、删除和发件均可用，重启后 MSAL 静默令牌获取正常。
- [ ] 撤销 Microsoft 授权或使令牌失效后，抽屉编辑窗口可原地重新授权，历史邮件不丢失。
- [ ] 重新授权误选另一个 Microsoft 账户时不覆盖当前账户。
- [ ] 安装与未安装 Microsoft Authenticator 两种环境均测试一次浏览器/broker 回调。

### OAuth 数据与隐私

- [ ] Room 5→6 覆盖升级后旧授权码账户、邮件、正文、联系人、发件箱和设置全部保留。
- [ ] 旧版 Gmail/Outlook/Microsoft 365 密码账户升级后保留本地邮件，但编辑窗口显示“重新授权邮箱”；后台不会把旧密码作为 XOAUTH2 Token 连接。
- [ ] 对上述旧账户完成 OAuth 双协议验证后，收信/发信恢复且本地历史不变；取消或选错账户时原账户不被覆盖。
- [ ] `accounts.oauthAccountId` 只保存服务商账户标识；数据库中没有 Access Token、Refresh Token 或 Client Secret。
- [ ] Debug 日志搜索 `Bearer`、`access_token`、`refresh_token`、`password`，不出现真实凭证。
- [ ] 删除 Gmail/Microsoft 本地账户时删除该账户本地数据；其他邮箱不受影响。

### HTML layout-v18

- [ ] 网易“新设备登录提醒”等短手机邮件以正常手机宽度显示，不再缩成页面中央的小卡片。
- [ ] Cloudflare 长 Newsletter 的 Logo、标题、段落、图表、按钮、页脚和右侧内容完整可见。
- [ ] Debug 日志中网易短邮件为 `FLUID`，Cloudflare 宽邮件为 `DESKTOP_SCALED`，桌面宽度不超过 1200px。
- [ ] 使用宽度只写在 `<style>` 中 `.wrapper/.container/.content` 规则的测试邮件，仍能识别桌面画布；未命中正文的无关 CSS 不会误分类。
- [ ] 桌面模式没有第二次 WebView overview 缩放，横向不出现永久裁切；双指缩放仍可用。
- [ ] GitHub 短邮件继续保持顺畅拖动，不因 layout-v18 回退。

### 构建与签名

- [ ] Debug 与 performance 构建可安装；两者使用相同 Debug 签名时 Google/Microsoft 登录均正常。
- [ ] 未来 Release 签名不与 Debug 相同时，Google 和 Microsoft 控制台已分别增加 Release 配置。
- [ ] R8 performance 包启动 OAuth 页面、MSAL 回调和 Google PendingIntent 不崩溃。

### 升级与数据

- [ ] 从未经修改的 v0.2.23.0 覆盖安装 v0.2.24.0，账户、邮件、正文缓存、联系人、发件箱、附件任务和设置仍存在。
- [ ] Room 从 v5 无损迁移到 v6，不出现 schema integrity、缺少迁移或清库。

---

## v0.2.23.0 专项回归

### 详情顶部安全区

- [ ] 在普通状态栏、挖孔/刘海和较高状态栏设备分别打开邮件，顶栏阴影不覆盖头像、发件人名称或第一行邮箱。
- [ ] 加载骨架和最终 HTML 的头像、三条发件人信息位于同一位置，交接时不向上/向下跳动。
- [ ] 发件人区域与正文之间约 10dp；顶部安全区随状态栏高度变化，不是所有设备固定 96dp。
- [ ] 隐藏/恢复详情顶栏后，正文顶部不残留空白条，也不穿到状态栏下方。

### GitHub 短邮件滚动

- [ ] 打开正文不足一屏的 GitHub 纯文本邮件，从内容区域缓慢向上拖动，正文有明确位移/过滚感，不像触摸被父层吞掉。
- [ ] 同一手势期间顶部栏和底部 Dock 能按方向收起；反向拖动能恢复。
- [ ] 连续短拖、长拖、快速 fling 和边缘拖动都不会中途停止响应。
- [ ] 返回列表再打开另一封邮件，旧邮件的 scrollY、触摸累计和 Chrome 状态不会带过去。
- [ ] Debug 日志能看到 `gesture travel`；若 scrollY 很小，仍能看到手指位移和 `canUp/canDown` 状态。

### Cloudflare / 桌面 Newsletter

- [ ] 打开 Cloudflare 对比邮件，Logo、标题、段落、图表、按钮、页脚和社交图标均能显示，右侧内容不被裁掉。
- [ ] 典型 600px newsletter 的 Debug 日志显示 `layout=DESKTOP_SCALED` 和合理的 `desktopWidth`。
- [ ] 桌面邮件整体缩小但内部列关系不散开，发件人头部仍保持正常字号。
- [ ] 普通 GitHub/Apple/纯文本邮件日志显示 `layout=FLUID`，文字不会被整个缩得过小。
- [ ] 横竖屏、不同系统字体大小下不出现永久横向滚动条；双指缩放仍可使用。

### K-9 式首次同步

- [ ] 清空 BondMail 数据后添加一个约 30–40 封历史邮件的测试账户，第一封先出现，最近 8 封以短间隔逐条进入，后续邮件继续分批出现。
- [ ] 列表不是长时间纯空白后一次性跳出全部邮件；已有缓存账户再次启动时也不会重复播放首次进入动画。
- [ ] Debug 日志先出现一次批量 `sync success ... newHeaders=...`，随后出现 `initial inbox visible` 和多条 `initial inbox batch`；没有为每封邮件重新连接 IMAP。
- [ ] 最近小邮件出现 `body batch success`，上限为 131072 字节；大邮件保持邮件头，首次打开时再下载正文。
- [ ] 最近邮件预览逐步补齐；列表正在拖动/fling 时，正文预取的 Room 更新不会造成明显掉帧。
- [ ] 添加账户过程复用已认证 Store 时能看到 `connect reuse`；失败重连仍只进行受控重试。
- [ ] 首次同步中途切后台、强制结束进程再打开，已入库邮件保留，剩余历史邮件继续作为基线，不发送历史通知。
- [ ] 首次同步完成后新到一封邮件，只通知真正新增的一封。

### 日志与隐私

- [ ] BondMail Debug 日志中没有授权码、应用专用密码、Access Token、Refresh Token 或完整正文。
- [ ] K-9 原始日志发送前已搜索并脱敏 `password/secret/token/incomingServerSettings/outgoingServerSettings`。
- [ ] 若 K-9 日志曾暴露客户端授权码，已在邮箱网页端撤销并重新生成。

### 升级与数据

- [ ] 从未经修改的 v0.2.22.0 覆盖安装 v0.2.23.0，账户、邮件、正文缓存、联系人、发件箱、附件任务和设置仍存在。
- [ ] Room 版本仍为 v5，不出现缺少迁移或 schema integrity 错误。
- [ ] Debug 与 performance 构建均可安装；Debug 可输出关键标签，performance 完成 R8 和资源压缩。

---

## v0.2.22.0 专项回归

### 下拉刷新与前后台恢复

- [ ] 首页下拉时列表有阻尼和跟手位移，但不显示圆形进度器。
- [ ] 未达到阈值松手只回弹；越过阈值有一次触觉反馈。
- [ ] 松手触发同步后列表归位，顶栏底部只显示一条 3dp 线性进度。
- [ ] 刷新尚未完成时切到后台，返回后旧加载状态不会一直存在。
- [ ] 后台期间旧同步失败，返回前台不会立即弹出遗留的“无法连接邮箱服务器”。
- [ ] 未完成首次同步的新账户重新进入前台后只安排一次安静恢复，不建立重复 IMAP 会话。

### Gmail 圆角、长按和未读层级

- [ ] 单条内容四角 12dp；第一条顶部 12dp/底部 5dp；中间四角 5dp；最后一条顶部 5dp/底部 12dp。
- [ ] 长按首项、中间项和末项，选中条目的四角都平滑形变为 12dp。
- [ ] 取消选中后圆角平滑恢复所在位置，形变不推动相邻条目。
- [ ] 邮件、联系人和搜索结果长按/按压的阴影、ripple 和裁切都是圆角形状，不出现正方形蒙层。
- [ ] 左右滑动背景与当前邮件 Shape 一致。
- [ ] 深色与 Monet 主题下，未读邮件可通过底色、左侧强调线、主色时间、圆点和粗体共同识别。
- [ ] 不同长度的时间都靠右，时间与星标共用固定右侧列且不会左右漂移。

### 顶栏顺序与返回

- [ ] 首页右侧顺序为“搜索 → 添加邮箱 → 更多”。
- [ ] 三个按钮滚动收起和返回时保持同一轨迹。
- [ ] 联系人或设置按返回先回邮件首页；首页再按返回才退出 Activity。
- [ ] 抽屉打开时返回仍优先关闭抽屉。

### 邮件正文滚动与移动端宽度

- [ ] GitHub 纯文本邮件从顶部开始短拖、连续拖和快速 fling 都能正常滚动，不像被父层抢手势。
- [ ] WebView 顶部和底部仍有系统过滚反馈，返回下一封邮件不会继承旧滚动监听。
- [ ] GitHub、Cloudflare、Grab、Bybit 和桌面 newsletter 在小屏上不裁掉右侧正文。
- [ ] 长链接、表格单元格、引用块和固定宽度模板能在可用宽度内断行。
- [ ] 图片/视频不超过屏幕，双指缩放仍可使用。
- [ ] 发件人头部与 App Bar 之间、头部与正文之间各有约 8dp 间距，骨架与最终页面位置一致。

### 写邮件与添加邮箱

- [ ] 写邮件默认展开约 72%，仍能看出来源页面且可见账户、收件人、主题和正文开头。
- [ ] 上滑全屏后 Sheet 顶部停在状态栏下方约 8dp，不覆盖时间和系统图标。
- [ ] Drag Handle 位于 Sheet 内部；部分展开和全屏按返回都直接关闭。
- [ ] 添加邮箱的 `@domain` 和展开图标始终贴右，短域名紧凑、长域名只向左扩展。
- [ ] 用户名区域获得焦点后能清楚看见主题主色输入光标。

### 首次同步通知基线

- [ ] 清空数据后添加一个有历史邮件的账户，历史邮件进入列表但不逐封发送系统通知。
- [ ] 首次同步完成后再收到一封新邮件，只通知这一封。
- [ ] 模拟 UIDVALIDITY 变化或重建最近窗口，不把整箱邮件重新通知。
- [ ] 首次同步中退后台、强制结束进程再重开，恢复同步后仍不把历史邮件当新消息。

### 升级与数据

- [ ] 从未经修改的 v0.2.21.0 覆盖安装 v0.2.22.0，账户、邮件、正文缓存、联系人、发件箱、附件任务和设置仍存在。
- [ ] Room 版本仍为 v5，不出现缺少迁移或 schema integrity 错误。

---

## v0.2.21.0 专项回归

### 选择模式退出

- [ ] 长按一封邮件进入选择模式，顶部显示“1 已选择”。
- [ ] 再次点击该邮件取消最后一个勾选，离场中的标题保持“1 已选择”并淡出，不出现“0 已选择”。
- [ ] 选中两封或更多邮件后逐个取消，非最后一次取消应实时更新数量，最后一次取消才退出选择模式。
- [ ] 选中背景短渐变恢复普通卡片颜色，不突然闪白、闪深或改变圆角。
- [ ] 使用系统动画缩放 0x、0.5x、1x、2x 各测试一次；0x 时不保留不可见选择层。

### 文件夹缓存与空状态

- [ ] 在收件箱、星标、已发送、草稿、垃圾邮件和回收站之间连续切换，不出现纯空白中间帧。
- [ ] 已有本地邮件的文件夹先显示 Room/内存缓存，再由同步结果更新。
- [ ] 确认为空的文件夹立即显示“暂无邮件”，不先空白再出现文案。
- [ ] 从抽屉同时切换账户和文件夹，不短暂显示旧账户或错误文件夹的数据。
- [ ] 快速连续点击多个文件夹，最终内容、选中 Chip、账户筛选和空状态一致。
- [ ] 进程重启后第一次进入已发送/草稿等文件夹，Room 缓存仍可直接显示。

### Gmail 分组圆角

- [ ] 只有一封邮件或一个联系人时，v0.2.21.0 基线四个角均为 24dp 大圆角。
- [ ] 多条内容的第一条只有顶部两角为大圆角，底部两角为 5dp。
- [ ] 中间条目四角均为 5dp。
- [ ] 最后一条只有底部两角为大圆角，顶部两角为 5dp。
- [ ] 邮件左右滑动时，底层操作背景与当前卡片使用完全相同的圆角。
- [ ] 搜索结果、联系人列表和普通邮件列表使用同一首尾规则。

### 全局 Monet Surface 层级

- [ ] 浅色模式下，状态栏/顶部标题区域与页面画布属于同一带色背景，邮件和联系人卡片明显分层。
- [ ] 深色模式下，页面、卡片、底部 Dock 和输入框之间有清晰但不过重的层级。
- [ ] 开启系统动态取色后，首页、联系人、设置、写邮件、详情、抽屉和添加邮箱的页面背景连续一致。
- [ ] 关闭动态取色后，固定浅色/深色主题仍保持相同语义层级。
- [ ] 在页面之间快速跳转，不出现某个页面突然使用纯白、纯黑或另一套 Surface。
- [ ] 底部 Dock 与写邮件按钮仍有独立悬浮层级，不与内容卡片融为一体。

### 添加邮箱单输入框

- [ ] QQ、163、126、iCloud、Yahoo 页面只显示一个邮箱地址外框。
- [ ] 左侧可正常输入用户名，右侧完整显示 `@domain` 和展开图标。
- [ ] Yahoo 长后缀在常见小屏宽度下不被不必要地截断。
- [ ] 点击右侧域名区域弹出菜单；只有一个后缀时不显示无意义的可点击状态。
- [ ] 粘贴完整且受支持的邮箱地址后，用户名和后缀自动拆分正确。
- [ ] 通用 IMAP/SMTP 粘贴完整邮箱时保留整个地址，不删除 `@` 后内容。
- [ ] 输入框获得焦点或域名菜单展开时只高亮同一个外轮廓。

### 升级与数据

- [ ] 从未经修改的 v0.2.20.0 覆盖安装 v0.2.21.0，账户、邮件、正文缓存、联系人、发件箱、附件任务和设置仍存在。
- [ ] Room 版本仍为 v5，不出现缺少迁移或 schema integrity 错误。
- [ ] Debug 与 performance 构建均可安装并打开；performance 包完成 R8 与资源压缩。

---

## v0.2.20.0 专项回归

### 首页搜索与通用滚动 Chrome

- [ ] 首页慢速向上拖动时，菜单、搜索、更多、添加邮箱保持同一水平基线并同步越过顶部边界。
- [ ] 首页快速 fling 隐藏顶栏时，搜索按钮不会单独提前、延后或走更长距离。
- [ ] 反向向下滚动时四个顶栏按钮以相同速度恢复；回到列表顶部后顶栏和底部 Dock 必须显示。
- [ ] 点击搜索时只看到一个搜索图标；展开和关闭均从真实搜索按钮位置开始/结束。
- [ ] 联系人向上滚动时顶部搜索栏和底部 Dock 同时收起，反向滚动同时回来。
- [ ] 设置页向上滚动时底部 Dock 收起，反向滚动恢复；设置页不额外出现固定标题。
- [ ] 在邮件、联系人、设置之间切换时，新页面默认恢复可见 Chrome，不继承上一个页面的隐藏位置。

### 邮件详情发件人首帧

- [ ] 冷启动后第一次打开 HTML 邮件，发件人名称和邮箱不发生由细变粗或由粗变细的瞬间切换。
- [ ] 已缓存邮件再次打开时也不闪出另一套发件人文字。
- [ ] 加载阶段只显示头像圆形和骨架线，不提前显示会与 WebView 重复的真实文字。
- [ ] 正文只在 WebView 最终排版完成后短渐显，不出现中间字体度量或邮箱截断状态。
- [ ] 使用 35～80 字符的长发件人邮箱测试，地址自然换行，尾部不显示 `...`，头部不裁切。
- [ ] 快速连续打开两封不同邮件，上一封的 VisualState 回调不会把旧内容闪到新邮件上。

### Gmail 内容区与按钮阴影

- [ ] 浅色模式邮件行使用 6dp 低圆角、3dp 紧凑间距和 10dp 左右留白，头像与三行文字层级接近对比图。
- [ ] 深色模式邮件卡片仍能与页面背景分层，不出现粗边框或过重阴影。
- [ ] 联系人行至少 78dp 高、6dp 圆角、3dp 间距，姓名与邮箱层级清晰。
- [ ] 浅色模式写邮件按钮四周存在完整外圈阴影，与左侧底部 Dock 属于同一悬浮层级。
- [ ] 浅色模式详情删除按钮四周存在相同阴影，不像平面贴图；深色模式轮廓仍清晰。
- [ ] 写邮件和删除按钮按压时使用相同缩放反馈，阴影不会被圆形边界裁掉。

### 升级与数据

- [ ] 从 v0.2.19.0 覆盖安装 v0.2.20.0，账户、邮件、正文缓存、联系人、发件箱和附件任务仍存在。
- [ ] Room 版本仍为 v5，不出现缺少迁移或 schema integrity 错误。

---

## v0.2.19.0 专项回归

### 邮件首帧与详情滚动

- [ ] 完全结束 App 进程后第一次打开 HTML 邮件，不出现整页白闪。
- [ ] 正文准备期间显示与当前浅色/深色主题一致的背景和发件人信息。
- [ ] 正文出现时只做短渐显，不发生尺寸跳变。
- [ ] 在高 DPI 手机上向上滚动，顶栏完全收回前已消耗正文顶部安全区。
- [ ] 顶栏隐藏后状态栏下方没有残留空白条或异色断层。
- [ ] Cloudflare、Bitget、Bybit、Grab、Apple 等测试邮件不被裁掉右侧。
- [ ] 转发/引用邮件不再只剩很窄的右侧文字列。

### 首页顶栏与刷新

- [ ] 抽屉、搜索、更多、添加邮箱均为同尺寸圆形容器。
- [ ] 四个按钮按压缩放和 ripple 节奏一致。
- [ ] 顶栏随列表滚动收回/返回时，搜索按钮和其他按钮同步移动。
- [ ] 搜索展开和关闭期间只出现一个搜索图标，不闪烁、不抽动。
- [ ] 下拉时显示跟手圆形刷新指示器。
- [ ] 释放刷新后圆形指示器收回，顶栏底部进度线持续到同步结束。
- [ ] 刷新进度不推动文件夹栏或邮件列表。

### 添加邮箱与页面配色

- [ ] Yahoo 多域名菜单悬浮在输入表单上，不把下方字段顶下去。
- [ ] `@yahoo.com`、`@yahoo.co.uk` 等完整可见，选中项勾选清晰。
- [ ] 首页、联系人、设置、写信、添加邮箱、抽屉和详情根背景没有明显跳色。
- [ ] 浅色、深色以及莫奈动态取色分别切换页面检查一次。

---

# v0.2.2 历史真机测试清单

## 编译与升级

- [ ] `adb devices` 能看到手机。
- [ ] `.\gradlew.bat clean installDebug` 能编译并覆盖安装。
- [ ] 从 v0.2.1 更新后原有账户和邮件仍存在。
- [ ] Room 1 → 2 → 3 或 2 → 3 迁移没有崩溃、没有清空账户。

## 邮件、联系人和底部导航

- [ ] 每封邮件显示为独立圆角块，块之间存在小间距。
- [ ] 未读邮件有粗体、浅色背景和小圆点。
- [ ] 联系人也显示为独立圆角块。
- [ ] 底部导航和内容区有独立背景及顶部细分隔线。
- [ ] 写邮件按钮只有较大的写信图标，没有文字。
- [ ] 点击底部导航不会左右滑动，只出现很轻的切换过渡。
- [ ] 邮件页和联系人页不能通过整页左右滑动切换。
- [ ] 多次切换邮件、联系人、设置后，滚动位置和搜索状态能够保留。

## 抽屉

- [ ] 首页左上角显示菜单按钮，不再显示“全部账户”按钮。
- [ ] 点击菜单后抽屉覆盖底部导航。
- [ ] 抽屉列出全部收件箱、每个账户、添加邮箱、文件夹和设置。
- [ ] 选择单个账户后只显示对应邮箱邮件。
- [ ] 关闭或返回抽屉动画流畅。

## HTML 邮件和正文缓存

- [ ] HTML 邮件保持排版、按钮、链接和图片位置，不退化成一段纯文本。
- [ ] Wi-Fi 图片策略开启时，HTTPS 图片能够加载。
- [ ] 邮件内 HTTP 图片也能在允许图片时加载。
- [ ] `cid:` 内嵌图片的常见邮件能够显示。
- [ ] 第一次打开旧 v0.2.1 HTML 邮件时会重新获取一次正文。
- [ ] 第二次打开同一邮件不再等待 IMAP 下载正文。
- [ ] 再次打开已缓存 HTML 时不显示长时间 WebView 加载条。
- [ ] 点击外部链接前仍会弹出确认。

## 同步与空列表修复

- [ ] 新绑定账户返回首页后立即开始出现邮件，不必等待 WorkManager。
- [ ] 邮件头逐封进入列表。
- [ ] 第一次只出现最近约 40 封，不下载全部历史正文。
- [ ] 旧版本曾经“刷新很快但一直暂无邮件”的账户能够自动重建最近窗口。
- [ ] 下拉刷新指示器约 1.2 秒内收起，后续邮件仍可继续逐封出现。
- [ ] 有新邮件时只增加新 UID。
- [ ] 全部账户连接失败时首页能显示连接错误，而不是静默空白。
- [ ] 第一次打开新邮件时下载正文；第二次打开使用 Room 缓存。

## 添加邮箱

- [ ] 点击服务商后进入独立账户输入页面。
- [ ] 邮箱域名后缀位于用户名输入框内部。
- [ ] 域名下拉菜单从后缀附近打开，不再出现突兀的大蓝色按钮。
- [ ] 连接按钮加载时尺寸不变化。
- [ ] 同一邮箱第二次添加时提示已经绑定。

## 页面动画与返回

- [ ] 添加邮箱、账户输入、邮件详情、写邮件均有统一进入动画。
- [ ] 点击返回时使用对应反向动画。
- [ ] Android 手势返回未松手时能看到上一级页面。
- [ ] 邮件列表左右滑动删除/未读操作不受主页面切换手势干扰。

## 多语言

- [ ] 简体中文、繁体中文、英文可切换。
- [ ] 三份 JSON 均为 119 个相同 key。
- [ ] 本轮新增界面没有新增 XML 业务文案或硬编码中文按钮。

---

# v0.2.12.0 专项回归

## 冷启动

- [ ] 先打开一次首页形成缓存，再强制停止 `com.bond.mail`。
- [ ] 冷启动时只看到系统启动画面，结束后直接进入缓存首页，不出现纯白中间帧。
- [ ] 深色系统下启动背景为深色，不先闪白。

## 下拉刷新

- [ ] 下拉超过阈值后，顶部 42dp 显示进度和“正在同步邮件”。
- [ ] 刷新尚未结束时立即向上滑动，列表不会盖到进度文字上。
- [ ] 刷新期间列表仍可正常滚动，结束后顶部空间平滑收回。

## HTML 邮件

- [ ] Bybit 邮件显示完整标题、Logo、正文和按钮，不再只有一小段。
- [ ] Grab 邮件右侧内容完整缩放到屏幕内，不需要横向猜测被截断的内容。
- [ ] GitHub 邮件保留 HTML 字号、卡片、按钮和图片占位逻辑，不退化为纯文本。
- [ ] 带 `name=message.html` 的 HTML 正文不会被识别为附件。
- [ ] 相对 `src` / CSS `url(...)` 在存在远程 `<base>` 时显示“显示远程图片”。
- [ ] 邮件内连续滚动不会触发永久加载层。
- [ ] 返回再打开同一封邮件，Debug 日志出现 `body cache hit`。

## 详情底部 Dock

- [ ] 回复、转发、分享位于左侧半透明圆角 Dock。
- [ ] 删除为右侧独立红色图标。
- [ ] 正文可从 Dock 背后透出，最后一行仍能滚动到 Dock 上方。

## 性能

- [ ] Debug 包用于抓日志：`.\gradlew.bat installDebug`。
- [ ] 帧率对比使用性能包：`.\gradlew.bat installPerformance`。
- [ ] 连续滚动 10 秒后重新导出 `dumpsys gfxinfo ... framestats`。
- [ ] 163 无新邮件连续刷新时，不再每次出现 `newHeaders=30`。

# v0.2.13.0 专项回归

1. 深色模式分别打开 Grab 回执、Grab 取消订阅、Apple、GitHub、Bybit 邮件，确认品牌颜色仍存在。
2. Grab 回执中“Detail Pesanan”后不应再出现整屏空白，字段应紧跟标题显示。
3. Grab 取消订阅和 Apple 标题应完整适配屏幕，不应横向超出或被发件人区域遮挡。
4. 连续上下滑动 Apple、GitHub 等长邮件，确认 WebView 不会卡死或突然回到加载状态。
5. 含远程图片且被阻止的邮件右上显示单个图片图标；点击后图标消失并加载图片。
6. 不含远程图片、CID 图片已内嵌或策略允许加载时，不显示悬浮图片图标。
7. 在邮件、联系人、设置之间连续切换，联系人页不应闪出“暂无联系人”。
8. 首页下拉刷新，确认“正在同步邮件”在同步区域中垂直居中且列表不重叠。


---

# v0.2.17.0 专项回归

## 构建与升级

- [ ] 从 v0.2.16.1 覆盖安装后账户、邮件、正文缓存和排序仍存在。
- [ ] Room 4→5 迁移成功，没有 `Room cannot verify the data integrity`。
- [ ] `assemblePerformance` 完成 R8 和资源压缩，没有 Missing class 错误。
- [ ] 性能 APK 明显小于旧的未压缩 performance 包。

## 底部导航和操作按钮

- [ ] 三个导航项的选中底色为完整椭圆，与点击 ripple 边界一致。
- [ ] 快速连续切 Tab 时指示器可中断，不会跳出 Dock。
- [ ] 写信按钮整颗使用强调色，没有内部第二层圆底。
- [ ] 详情删除按钮为完整错误色容器。

## 删除确认

- [ ] 详情删除会先出现确认对话框。
- [ ] 批量删除显示正确选中数量。
- [ ] 取消确认后邮件不消失。
- [ ] 删除邮箱会显示本地删除说明；取消后账户仍存在。

## 邮件手势

- [ ] 右滑未读邮件后变已读。
- [ ] 再次右滑已读邮件后变未读。
- [ ] 左滑未星标邮件后加星标。
- [ ] 再次左滑已星标邮件后取消星标。
- [ ] 正常上下滚动和斜向滚动不会误触左右操作。
- [ ] 惯性滚动停止前不能触发左右操作。

## 写邮件 Bottom Sheet

- [ ] 首页写信初次打开约占屏幕 80%。
- [ ] 上滑可以展开为全屏。
- [ ] 全屏返回先退回 80%，再次返回关闭。
- [ ] 80% 状态下向下拖动可以关闭。
- [ ] 联系人、回复、转发打开同一套抽屉动画。
- [ ] 打开/关闭期间来源页面不闪白、不重新创建。

## 附件

- [ ] 可以一次选择多个文件，最多保留 10 个。
- [ ] 附件名称显示正确，单个附件可移除。
- [ ] 带附件邮件进入发件箱后关闭 App，Worker 仍能读取并发送。
- [ ] 收件端能看到正文和全部附件，文件名及 MIME 类型正常。
- [ ] 发送失败后重试仍可读取附件 URI。

## 首页抽屉

- [ ] 不再显示顶部“邮箱”标题块和“邮箱账户”分组标题。
- [ ] 第一项显示“所有邮箱”。
- [ ] 1～3 个邮箱时不显示展开按钮。
- [ ] 超过 3 个邮箱时默认显示前三个，并可展开/收起。
- [ ] 长按拖动手柄能调整邮箱顺序。
- [ ] 重启后顺序保持，写信账户列表与抽屉顺序一致。
- [ ] 编辑图标可修改邮箱名称，最长 32 个字符。
- [ ] 删除图标需要确认，删除后本机消息/凭证清理。

## 搜索与顶部按钮

- [ ] 搜索图标扩展和收回时不会出现双图标或抽动。
- [ ] 搜索容器完成展开后才弹出键盘。
- [ ] 搜索、更多、添加邮箱三个按钮尺寸和背景一致。
- [ ] 右上角添加按钮可以进入添加邮箱流程。
