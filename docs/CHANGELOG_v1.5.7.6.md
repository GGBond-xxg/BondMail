# BondMail v1.5.7.6

版本代码：171。支持覆盖安装。

- 修复冷启动首次打开邮件时，进入动画左侧列表背景短暂闪黑的问题。
- 邮件页面使用独立合成和边界裁剪，防止首次 WebView 绘制影响底下的列表。
- 进入动画期间保留列表快照，动画结束即释放；返回时仍显示实时邮件列表。

验证：Performance 构建与 lintVital 通过；已在连接的 vivo 上完成两次冷启动首次邮件逐帧检查和返回检查，未再观察到左侧背景缺帧。其他机型仍需实际使用反馈。
